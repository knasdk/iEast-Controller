{
  "name": "ieast-controller",
  "version": "1.0.0",
  "private": true,
  "description": "Local web controller for an iEast/LinkPlay streamer",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
